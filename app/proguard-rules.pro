# App-specific ProGuard/R8 rules. The AndroidX and Material libraries ship
# their own consumer rules, so nothing extra is needed yet. Add keep rules here
# when reflection-based libraries (e.g. Gson, Firebase) join the project.
